steps:
  # Install
  - name: 'gcr.io/cloud-builders/npm'
    args: ['install']

  # Install functions
  - name: 'gcr.io/cloud-builders/npm'
    dir: 'functions'
    args: ['install']

  # Deploy
  - name: 'gcr.io/fm-resume/firebase'
    args: ['deploy']
